/**
 * Divide the page into two equal parts. 
 * Each part should be scrollable vertically.
 *  Each part should be resizable horizontally(i. e We can Increase or Decrease the size of each part).

 */